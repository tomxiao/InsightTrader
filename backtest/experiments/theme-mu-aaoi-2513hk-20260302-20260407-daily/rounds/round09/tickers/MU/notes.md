## 本轮目标

- 在不破坏 round08 已修复的偏多识别能力前提下，减少 `MU` 的两段式错误：
- 强势回升时不要压成 `hold`
- 转弱后不要继续给过密 `buy_on_pullback`

## 改动内容

- 在 prompt 里补了更细的边界：
- 已从支撑区回升、修复启动时，更容易回到 `趋势延续`
- 连续反弹失败、关键位失守后，不应继续拿“健康回调”维持 `趋势延续`

## 观察结论

- `MU` 是本轮唯一略有改善的样本。纯信号从 round08 的 `9/11/6` 提升到 `10/10/6`。
- 关键改动是：`2026-03-13` 从 `hold bad_case` 修到了 `buy_on_pullback good_case`，`2026-03-25` 从 `hold bad_case` 修到了 `sell good_case`。
- 但同时也引入了新的回撤：`2026-03-11` 从 `buy_on_pullback good_case` 又掉回 `hold bad_case`。
- 整体看，这轮对 `MU` 只是小修，并没有形成足够稳定的净改进。

## 下一步

- `MU` 不需要再靠大范围状态边界微调来追求改善。
- 如果继续做信号层 round，更适合把它作为次级约束，而不是主攻样本。
