## 本轮目标

- 在保持港股样本纯信号质量的同时，继续压缩 round08 剩余的反弹期误卖。

## 改动内容

- prompt 中进一步强调：
- 高波动和高风险不等于方向已经偏空
- 若主叙事更像反弹修复或支撑博弈，应优先在 `趋势延续 / 震荡等待确认` 中判断，而不是直接给 `sell`

## 观察结论

- 纯信号上，`2513.HK` 略有改善：从 round08 的 `9/11/4` 到 round09 的 `10/12/2`，准确率基本持平略升。
- 但动作分布明显变差：`buy_on_pullback` 从 `8` 降到 `5`，`hold` 从 `7` 升到 `12`，实际成交重新掉到 `0`。
- 也就是说，这轮虽然没有明显拉低方向准确率，却重新引入了港股样本的过度保守问题。

## 下一步

- `2513.HK` 仍然必须作为 guardrail 使用。
- 下一轮任何 prompt 修改，都不应再接受它重新掉到 `0` 成交或 `hold` 明显膨胀的结果。
