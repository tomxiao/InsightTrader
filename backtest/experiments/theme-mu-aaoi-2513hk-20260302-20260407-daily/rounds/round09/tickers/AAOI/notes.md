## 本轮目标

- 继续减少 `AAOI` 在趋势性上涨中的 `hold` 滥用，争取把 round08 已提升到 `8/15` 的方向质量继续往上推。

## 改动内容

- 复用 round03 analyst 输入，只改 decision manager 的状态边界。
- 重点尝试让“已开始修复的偏多结构”不要再停留在 `震荡等待确认`。

## 观察结论

- 这是本轮最明确的失败样本。纯信号从 round08 的 `8/15/3` 退回到 `5/17/4`。
- 多个在 round08 已经修好的日期被重新压回 `hold bad_case`，例如：
- `2026-03-04`
- `2026-03-09`
- `2026-03-19`
- `2026-04-02`
- 这说明 round09 的 prompt 收缩并没有减少 `AAOI` 的 `hold` 滥用，反而让它复发了。
- 执行层也明显恶化：平均收益从 `-1.5098` 退到 `-6.6904`。

## 下一步

- 下一轮如果继续做信号层，`AAOI` 应重新成为主攻样本。
- 目标不是更细地平衡 `buy / hold / sell`，而是更明确地压住“上涨修复中仍然观望”的模式。
