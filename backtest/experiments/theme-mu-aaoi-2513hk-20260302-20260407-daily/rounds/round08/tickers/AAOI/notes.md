## 本轮目标

- 验证 round08 的状态边界修正，是否会把 `AAOI` 从 round07 的“方向持平但执行偏弱”进一步推向更稳定的纯信号改善。

## 改动内容

- 复用 round03 analyst 输入，只改 `decision manager` 的状态边界。
- 重点让“高风险但未必偏空”的情形，不再轻易落成 `风险主导`，而是重新区分成 `趋势延续` 或 `震荡等待确认`。

## 观察结论

- `AAOI` 是本轮最明显的正向样本之一。在新的未来 3 日均价口径下，纯信号标签为 `8/15/3`。
- 动作分布从 `9/13/4` 变成 `14/10/2`，即 `buy` 增加、`sell` 收回，方向判定明显重新偏向“偏多但择机参与”。
- 多个 round07 的 `hold bad_case` 被修正成了 `buy_on_pullback good_case`，例如 `2026-03-09`、`2026-03-16`、`2026-03-19`、`2026-04-02`。
- 执行层也比 round07 明显改善：平均收益从 `-10.0406` 回到 `-1.5098`，但仍未达到稳定可用水平。

## 下一步

- `AAOI` 的纯信号方向已经明显改善，可以视为这轮 prompt 修正成功的核心证据之一。
- 下一步如果继续做 signal-only 优化，只需要防止 `buy` 再次过度放宽；如果转入执行层优化，`AAOI` 很适合拿来验证入场区和触发条件是否能把 `-1.5098` 继续推向正值。
