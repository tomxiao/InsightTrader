## 本轮目标

- 在保持 round07 纯信号改善的前提下，修复它过度偏空、`sell` 过多的问题。

## 改动内容

- 在状态边界中明确：高波动、宏观风险、公司新闻静默、财务脆弱性本身，不足以自动推出 `风险主导`；只有未来 `1-2` 周的防守需求已经明显压过进攻需求时，才允许给 `建议卖出`。

## 观察结论

- 这是 round08 最关键的平衡性改善样本。在新的未来 3 日均价口径下，纯信号标签为 `good/bad/unclear = 9/11/4`。
- 同时动作分布显著回归均衡：`buy_on_pullback` 从 `2` 回升到 `8`，`hold` 从 `6` 回到 `7`，`sell` 从 `16` 降到 `9`。
- 执行层也重新恢复有效性：`trade_count = 2`，`triggered_trade_count = 3`，`avg_return = 2.2778`，比 round07 的 `0` 成交明显更健康。
- 这说明 round08 成功保住了 round07 对港股样本的纯信号修复，同时去掉了其中最极端的偏空副作用。

## 下一步

- 如果继续做 signal-only 优化，`2513.HK` 不再需要大改方向边界，只需清理剩余几天 `hold/sell bad_case` 的细小误判。
- 如果进入执行层优化，`2513.HK` 很适合作为港股 guardrail：要求后续优化不能再把它推回 `sell` 过多或 `0` 成交。
