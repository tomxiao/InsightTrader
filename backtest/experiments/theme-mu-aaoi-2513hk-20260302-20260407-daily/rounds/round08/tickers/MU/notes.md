## 本轮目标

- 修复 round07 把“趋势中的健康回调”误降为 `保持观望` 的问题，尤其是财报前支撑区测试与整理阶段。

## 改动内容

- 在 signal-first prompt 中补充状态边界：若上涨逻辑未破坏、价格处于健康回调或关键支撑测试、正文核心仍是“等更好位置参与”，应优先落在 `趋势延续 -> 择机买入`，而不是 `震荡等待确认 -> 保持观望`。

## 观察结论

- 这是一个明确改善但尚未彻底修好的 round。在新的未来 3 日均价口径下，纯信号标签为 `good/bad/unclear = 9/11/6`。
- 动作分布也更接近我们想要的形态：`buy_on_pullback` 从 `4` 回到 `10`，`hold` 从 `15` 降到 `10`，`sell` 从 `7` 降到 `6`。
- 最重要的修复发生在 `2026-03-10`、`2026-03-11`、`2026-03-12`：这些日期从 round07 的 `hold bad_case` 重新变成 `buy_on_pullback good_case`，说明“趋势未坏但需等位置”的场景已经能更稳定识别出来。
- 但执行层仍弱，实际成交 `7` 笔全部亏损，平均收益 `-6.5776`。这说明 `MU` 当前更像“信号层明显修回，执行层仍拖后腿”。

## 下一步

- 如果继续做纯信号 round，`MU` 的重点不再是大幅调整状态边界，而是清理剩余的 `buy_on_pullback bad_case` 与少数 `hold/sell bad_case`。
- 如果进入第二阶段，`MU` 应优先做入场区和触发条件优化，因为当前主要问题已经不是方向判断，而是买入执行质量。
