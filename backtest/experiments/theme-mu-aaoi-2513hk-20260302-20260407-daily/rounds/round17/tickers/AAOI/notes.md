## 本轮目标

- 验证“催化后高波动整理型”的内部分型，能否在保留 round13 修复收益的同时，避免再把 `AAOI` 过早推向 `sell`。

## 改动内容

- 把 `AAOI` 允许落入“催化后高波动整理型”，强调不要因为过热、波动率抬升或利好已兑现一部分，就立刻切到 `风险主导`。
- 本轮中途补了一条纯格式约束，并只重生了 `2026-04-01` 的 summary。

## 观察结论

- `AAOI` 是本轮最主要的失败来源，从 round13 的 `10/14/2` 掉到 `7/16/3`，准确率只剩 `30.4%`。
- 最明显的新坏模式不是“乱卖”，而是 **`sell` 被彻底抹掉了**，动作分布变成 `buy=14 / hold=12 / sell=0`。
- 结果上，几天 round13 的关键修复全部退回了 `hold bad_case`：
- `2026-03-02`：`buy_on_pullback good_case -> hold bad_case`
- `2026-03-09`：`buy_on_pullback good_case -> hold bad_case`
- `2026-03-20`：`buy_on_pullback good_case -> hold bad_case`
- 这说明“催化后高波动整理型”这条分型在当前写法下过度强调了“先消化、别急着下偏空结论”，但副作用是把真正该积极参与的强势样本也推回了 `hold`。
- 执行层平均收益也维持在负值 `-5.9277`，进一步说明这不是“标签误差”，而是方向判断本身在退步。

## 下一步

- 如果继续使用分型，`AAOI` 不能再被整体塞进“催化后高波动整理型”的默认框架。
- 更合理的做法是只保留“不要过早判成 `sell`”这一条窄规则，同时重新允许“催化未破坏 + 回调后继续强”的日期回到 `buy_on_pullback`。
