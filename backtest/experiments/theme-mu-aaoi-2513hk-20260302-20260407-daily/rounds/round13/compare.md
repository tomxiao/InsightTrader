# round13

## 本轮目标

- 明确回到 `round11` baseline，只修 `AAOI` 一类残留误差：
- 对“催化刚落地后的首轮过热回撤”，如果报告核心只是“别追高 / 保护利润 / 等待消化”，而不是“结构已坏 / 反弹只是卖点 / 未来 1-2 周应撤退”，就不要轻易给 `风险主导 -> 建议卖出`。
- 同时要求不要重演 round12 那种把 `hold` 全局抬高的副作用。

## 跨标的总结

- round13 在主题主指标上极窄幅超过了 round11。按统一的未来 `3` 个交易日收盘均价口径，全主题达到 `good/bad = 33/34`，方向准确率 **49.3%**，略高于 round11 的 `49.2%`。
- `AAOI` 是本轮主要贡献来源，从 round11 的 `8/15/3` 提升到 `10/14/2`，准确率从 `34.8%` 提到 `41.7%`。最关键的修复是：
- `2026-03-02`：`hold bad_case -> buy_on_pullback good_case`
- `2026-03-09`：`sell bad_case -> buy_on_pullback good_case`
- `2513.HK` 也同步改善，从 round11 的 `12/7/5` 提升到 `15/8/1`，执行层平均收益升到 `28.6635`。
- 但 `MU` 明显回撤，从 round11 的 `11/10/5` 掉到 `8/12/6`。几个 round11 的好样本被重新压坏，例如 `2026-03-10`、`2026-03-13` 从 `buy_on_pullback good_case` 退回 `hold bad_case`。
- 结论：如果只看全主题准确率，round13 是新的 theme-high；但如果看分标的平衡性，round11 仍然更稳。round13 更适合作为“当前主指标最佳 candidate”，而不是无条件替换 round11 的唯一基线。
- 下一轮若继续推进，更合理的 round14 方向不是继续放大这条规则，而是以 round13 为出发点，只补 `MU` 的 guardrail，防止健康回调再次被压成 `hold`。
