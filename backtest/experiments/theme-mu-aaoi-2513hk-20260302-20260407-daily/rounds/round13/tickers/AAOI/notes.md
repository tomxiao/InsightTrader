## 本轮目标

- 只修 `AAOI` 在“催化刚落地后的首轮过热回撤”里被过早判成 `sell` 的误差。

## 改动内容

- prompt 回到 round11 骨架，只增加一条更窄规则：
- 若报告核心只是“别追高 / 保护利润 / 等待消化”，而不是“结构已坏 / 反弹只是卖点 / 未来 1-2 周应撤退”，则不要轻易从催化后的偏多整理升级到 `风险主导 -> 建议卖出`。

## 观察结论

- `AAOI` 明显改善，从 round11 的 `8/15/3` 提升到 `10/14/2`。
- 最关键的修复是：
- `2026-03-02`：`hold bad_case -> buy_on_pullback good_case`
- `2026-03-09`：`sell bad_case -> buy_on_pullback good_case`
- 这说明 round13 的新规则确实命中了“催化后首轮回撤不应过早偏空”的误判。
- 但副作用也存在：
- `2026-03-19`：`buy_on_pullback good_case -> hold bad_case`
- `2026-03-05`、`2026-03-18` 虽然不再是 `sell`，但只落到 `hold bad_case`，没有真正修成好样本。
- 动作分布变成 `buy=19 / hold=7 / sell=0`，说明这条规则把 `AAOI` 的偏空分支几乎全部抹掉了。
- 执行层平均收益改善到 `-3.4161`，但仍然为负。

## 下一步

- round14 不应继续单边削掉 `sell`。
- 更合理的是保留这轮对 `2026-03-09` 一类样本的修复，同时重新补一条更窄 guardrail：当回调已经演变成连续破位或反弹仅是减仓机会时，仍应允许 `sell` 存在。
