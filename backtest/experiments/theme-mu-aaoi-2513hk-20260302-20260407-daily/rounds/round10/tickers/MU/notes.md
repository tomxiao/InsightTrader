## 本轮目标

- 在不破坏 round08 已有结构的前提下，观察“高位强趋势股整理不应机械观望”这条窄规则，是否能让 `MU` 少一些错误 `hold`。

## 改动内容

- decision manager 回到 round08 骨架，只加了一条针对高位强趋势整理样本的窄规则：若核心建议仍是顺势等待更好位置参与，不要直接压成 `保持观望`。

## 观察结论

- `MU` 是本轮明确改善样本。纯信号从 round08 的 `9/11/6` 提升到 `10/10/6`。
- 典型修复包括：
- `2026-03-09`：从 `hold bad_case` 修到 `buy_on_pullback good_case`
- `2026-03-25`：从 `hold bad_case` 修到 `sell good_case`
- 同时也引入了新的过度偏多：`buy_on_pullback` 从 `10` 激增到 `17`，坏样本里 `buy_on_pullback` 已占到 `7` 个。
- 执行层虽然改善到 `-5.5415`，但仍明显为负。

## 下一步

- `MU` 不需要再大幅放宽 `buy`。
- 下一轮若继续沿 round10 前进，必须限制 `MU` 的 `buy_on_pullback` 继续膨胀。
