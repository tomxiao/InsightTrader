# round20

## 本轮目标

- 在 round18/19 两轮 MU 逻辑失败后，明确 pivot 回 round13 的强项：
- 只验证一个更窄的 AAOI 类假设：当催化刚落地、正文明确给出回调参与区或支撑企稳后参与条件、且没有把反弹定义为减仓机会时，更稳定地保留 `趋势延续 -> 择机买入`。

## 跨标的总结

- round20 相比 round18/19 有所回升，总体 `good/bad/unclear = 27/39/10`，方向准确率 **40.9%**。
- 它仍然明显低于 `round13` 的 **49.3%** 和 `round11` 的 **49.2%**，所以仍是失败 round。
- `2513.HK` 是本轮主要改善来源，从 round19 的 `9/13/2` 回到 `12/10/2`，执行层也恢复到 `2` 笔成交、`27.7392` 的平均收益。
- `MU` 没有恢复，仍只有 `9/11/6`。
- `AAOI` 进一步恶化到 `6/18/2`，说明这条 pivot 没能复制 round13 对 AAOI 的真实优势。
- 结论：在这次从 round13 出发的 3-round 迭代里，最好的尝试也只到 `40.9%`，说明当前这条“再加一条 prompt 规则”的路线已经明显低于 round13 candidate 的上限。
