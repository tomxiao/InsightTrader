## 本轮目标

- 检查 round05 对港股 guardrail 的放松规则，是否会破坏 `AAOI` 在 round04 上已经获得的收益改善。

## 改动内容

- 复用 round03 的 `1_analysts`，仅重跑 `decision manager`。
- 保留 round04 的“补风险主导卖出”方向，同时新增“技术计划不应被公司新闻静默机械否决”的例外规则。

## 观察结论

- `buy_on_pullback` 从 round04 的 `11` 降到 `9`，`sell` 从 `2` 升到 `3`，但动作分布改善并没有转化成结果改善。
- 仅有 `1` 笔实际成交，且亏损 `-14.6937`；平均收益从 round04 的 `8.0742` 大幅恶化。
- 在新的纯信号标签下，`good_case / bad_case / unclear = 4 / 21 / 1`，相较 round04 的 `5 / 20 / 1` 依旧更差，说明这轮规则对 `AAOI` 在信号层面也是明显负向的。
- 当前看，`AAOI` 更依赖 round04 那种更严格的“弱买入压制 + 风险主导卖出”边界；round05 对技术计划的放松不适合直接套到这类美股高波动样本上。

## 下一步

- 下一轮应优先回退 `AAOI` 上被放松的例外逻辑。
- 如果要继续保留 guardrail 修正，必须把适用范围压缩到“价格已逼近关键支撑、文本明确等待当前附近企稳”的更窄场景。
