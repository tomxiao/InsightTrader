## 本轮目标

- 验证 round05 新增的“技术计划不应被公司新闻静默机械否决”规则，是否会影响 `MU` 上原本已经收紧的弱买入。

## 改动内容

- 复用 round03 的 `1_analysts`，只重跑 `decision manager`。
- 新规则允许在高波动样本中，当技术结构、参与区和失效条件都较清晰时，不因“公司新闻静默 / 基本面不够漂亮”而自动压成 `保持观望`。

## 观察结论

- `buy_on_pullback` 从 round04 的 `8` 回升到 `9`，`hold` 维持在 `13`，`sell` 从 `5` 降到 `4`。
- 实际成交从 `5` 笔降到 `2` 笔，但两笔都亏损，平均收益为 `-5.2658`，比 round04 的 `-4.8567` 更差。
- 在新的纯信号标签下，`good_case / bad_case / unclear = 6 / 13 / 7`；相较 round04 的 `7 / 13 / 6`，坏信号没有减少，好信号反而少了一个。
- 这说明 round05 对 `MU` 的问题不是“信号方向大幅恶化”，而是没有真正提高信号质量，同时还牺牲了部分有效信号。

## 下一步

- 下一轮应继续保持 `MU` 的收弱买入逻辑，不要再让“未来可能回调后参与”自动转成 `择机买入`。
- 如果还要保留港股 guardrail 的例外，必须加上“当前价格已回到关键参与区附近”的限制，避免 `MU` 再次被放宽。
