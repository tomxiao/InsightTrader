## 本轮目标

- 作为港股 guardrail，验证 round05 是否能修复 round04 把 `2513.HK` 过度压成 `保持观望` 的问题。

## 改动内容

- 复用 round03 的 analyst 报告，只重跑 `decision manager`。
- 新增一条更宽松的判断：当技术结构未破坏，且参与区、失效条件较清晰时，不因公司新闻静默或基本面不够漂亮而机械退回 `保持观望`。

## 观察结论

- `buy_on_pullback` 从 round04 的 `3` 进一步压到 `2`，但恢复了 `1` 笔实际成交，且该笔收益为 `18.9313`。
- 在新的纯信号标签下，`good_case / bad_case / unclear = 4 / 16 / 4`，和 round04 完全相同。
- 这说明 round05 对 `2513.HK` 的收益改善主要来自执行层，而不是信号方向本身；从纯信号角度看，它只是避免了继续恶化，并没有真正改善。

## 下一步

- 下一轮应保留这条修正的核心方向，但必须进一步加上更严格的触发条件。
- 重点是只在“当前价格已回到关键参与区附近”的样本上启用该例外，避免它外溢到 `MU / AAOI`。
