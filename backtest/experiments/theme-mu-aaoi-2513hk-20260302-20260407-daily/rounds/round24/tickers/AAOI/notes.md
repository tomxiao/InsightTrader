## 本轮目标

- 本轮不主动修 `AAOI`，主要验证 `MU` 局部规则不会明显伤到 `AAOI`。

## 改动内容

- 沿用 `round22` 的结构化辅助层框架，只加入服务 `MU` 修复期语境的极窄 tie-breaker。

## 观察结论

- `AAOI` 维持在 `11/14/1`，准确率 `44.0%`，与 `round22` 持平。
- 动作分布仍是 `buy=20 / hold=6 / sell=0`，说明这条 `MU` 局部规则没有明显向 `AAOI` 外溢。
- 当前 `AAOI` 剩余坏样本仍集中在两类：
- 趋势延续下的过早买入：如 `2026-03-11`、`2026-03-12`、`2026-03-13`、`2026-03-16`
- 催化后整理期的 `hold bad_case`：如 `2026-03-18`、`2026-03-19`、`2026-03-20`

## 下一步

- 若后续要继续冲击 `55%`，`AAOI` 仍是值得单独开局部 round 的样本，但应避免再和 `MU` 修正规则混在一起。
