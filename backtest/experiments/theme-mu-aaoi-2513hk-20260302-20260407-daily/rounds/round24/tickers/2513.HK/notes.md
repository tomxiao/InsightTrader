## 本轮目标

- 本轮不直接修 `2513.HK`，主要观察 `MU` 局部修正是否会再次伤害港股 beta 样本。

## 改动内容

- 沿用结构化辅助层，不改 beta 分型定义；只补 `MU` 修复期的局部 tie-breaker。

## 观察结论

- `2513.HK` 从 `round22` 的 `9/11/4` 提升到 `9/9/6`，准确率来到 `50.0%`。
- 动作分布从 `buy=9 / hold=10 / sell=5` 变成 `buy=13 / hold=7 / sell=4`，说明这轮并没有把港股样本重新压回保守，反而顺手修回了一部分偏多样本。
- 当前剩余坏样本主要集中在：
- `Beta情绪技术驱动型 -> hold bad_case`
- `Beta情绪技术驱动型 -> sell bad_case`
- 以及少量 `趋势延续 -> buy_on_pullback bad_case`

## 下一步

- 如果后续继续推进，`2513.HK` 值得作为 guardrail 保留；当前看，这条 `MU` 局部修正规则对它没有明显副作用。
