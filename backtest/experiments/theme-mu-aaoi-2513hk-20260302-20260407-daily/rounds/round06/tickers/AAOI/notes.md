## 本轮目标

- 检查更严格的例外收窄规则，是否能让 `AAOI` 回到比 round05 更好的纯信号分布。

## 改动内容

- 复用 round03 的 `1_analysts`，只重跑 `decision manager`。
- round06 将 round05 的技术计划例外进一步压窄，只允许在更接近首选参与区的场景中保留 `择机买入`。

## 观察结论

- `buy_on_pullback` 从 round05 的 `9` 降到 `5`，`hold` 升到 `19`，`sell` 回到 `2`。
- 回测层面只剩 `1` 笔成交，平均收益 `-5.0`，较 round05 的 `-14.6937` 有所恢复，但仍远差于 round04 的 `8.0742`。
- 在纯信号标签下，`good/bad/unclear = 3/21/2`，比 round04 的 `5/20/1` 和 round05 的 `4/21/1` 都更弱。
- 说明 round06 对 `AAOI` 的主要效果是“更多观望”，而不是“更好的方向判断”。这不符合我们的 round06 目标。

## 下一步

- `AAOI` 不适合继续依赖这条港股 guardrail 式的 prompt 例外调整。
- 下一轮应单独回到 `AAOI` 的风险主导识别问题，重新看哪些日期应该是更稳定的 `sell`，哪些才是真正该保留的 `buy_on_pullback`。
