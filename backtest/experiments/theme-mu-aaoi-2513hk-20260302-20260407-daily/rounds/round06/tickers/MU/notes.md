## 本轮目标

- 验证更严格的港股例外收窄规则，是否能在不破坏 `MU` 信号质量的前提下，继续压住弱买入。

## 改动内容

- 复用 round03 的 `1_analysts`，仅重跑 `decision manager`。
- 将 round05 的“技术计划例外”进一步收窄：只有当前价格已落入、逼近或正在测试首选参与区时，才允许例外转成 `择机买入`。

## 观察结论

- `buy_on_pullback` 从 round05 的 `9` 降到 `6`，`hold` 升到 `16`，`sell` 维持 `4`。
- 回测层面只剩 `1` 笔成交，平均收益 `-4.6341`，比 round05 的 `-5.2658` 略好，但仍弱于信号层结论。
- 在纯信号标签下，`good/bad/unclear = 2/18/6`，明显差于 round04 的 `7/13/6` 与 round05 的 `6/13/7`。
- 说明这轮对 `MU` 收得过头了：虽然买点数量下降，但并没有提升信号方向质量，反而把更多样本推成了错误或错失型 `hold`。

## 下一步

- 不要再沿着“继续收紧例外”这条线对 `MU` 细调。
- 下一轮更适合先单独定义 `MU` 的纯信号目标，例如优先减少哪一类 `hold bad_case` 与哪一类 `buy bad_case`，再决定是否需要新的状态边界。
