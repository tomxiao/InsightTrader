## 本轮目标

- 在不外溢到美股样本的前提下，测试“极窄版港股例外规则”能否继续保住 `2513.HK` 的 signal quality。

## 改动内容

- 复用 round03 的 analyst 报告，仅重跑 `decision manager`。
- round06 要求：只有当前价格已落入、逼近或正在测试首选参与区，并且文本明确是“当前附近等待企稳参与”时，才允许保留 `择机买入`。

## 观察结论

- `buy_on_pullback` 维持在 `2`，`hold` 从 round05 的 `15` 升到 `16`，`sell` 回到 `6`。
- 实际成交重新掉回 `0`，说明港股样本的过度保守问题又被带回来了。
- 在纯信号标签下，`good/bad/unclear = 3/17/4`，比 round04/05 的 `4/16/4` 更差。
- 这说明 round06 的收窄方式已经过头：它虽然避免了 round05 的规则外溢，但也把 `2513.HK` 本来应保住的少量有效买点一起压掉了。

## 下一步

- 下一轮不应继续靠 prompt 中的“更细例外”来修 `2513.HK`。
- 更合理的做法是把港股 guardrail 的优化从“状态边界”里拆出来，改成单独的信号评估或区间层规则，否则会继续在 `MU / AAOI / 2513.HK` 之间来回拉扯。
