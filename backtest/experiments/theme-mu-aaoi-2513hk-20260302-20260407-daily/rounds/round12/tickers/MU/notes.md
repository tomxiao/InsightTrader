## 本轮目标

- 作为 guardrail，验证专修 `AAOI` 的支撑测试规则不会明显破坏 `MU` 在 round11 的改善。

## 改动内容

- 与其他样本一致，沿用 round11 prompt，只增加“若当前价格尚未真正进入报告认可支撑区，就不要轻易继续维持 `buy_on_pullback`”的窄规则。

## 观察结论

- `MU` 明显被这条规则拖回了保守侧：从 round11 的 `11/10/5` 回落到 `10/10/6`。
- 动作分布从 `13/7/6` 收缩为 `7/12/7`，说明 prompt 把很多原本的偏多样本直接压回了 `hold`。
- 典型回撤包括：
- `2026-03-10`：`buy_on_pullback good_case -> hold bad_case`
- `2026-03-13`：`buy_on_pullback good_case -> hold bad_case`
- 虽然也有少量正向变化，如 `2026-03-25` 从 `hold bad_case` 修成 `sell good_case`，但整体上这轮对 `MU` 是负贡献。
- 执行层平均收益略改善到 `-5.2658`，但纯信号层没有变好。

## 下一步

- round13 不应继续沿这条规则做更保守的扩张。
- `MU` 应继续只作为回归测试样本，防止为了修 `AAOI` 再次把健康回调压成大量 `hold`。
