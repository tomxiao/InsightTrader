## 本轮目标

- 只修 `AAOI` 剩余的“当前价格是否已经真正进入支撑测试区”这类误判，避免在支撑仍明显低于现价时过早给 `buy_on_pullback`。

## 改动内容

- 在 round11 的 signal-first prompt 上新增一条更窄规则：
- 对催化后高波动整理样本，若报告认可的参与区仍明显低于当前价格、正文也未说明支撑测试已在当前价位附近展开，则不要仅靠旧催化继续维持 `趋势延续 -> 择机买入`。

## 观察结论

- 这轮没有改善 `AAOI`，反而从 round11 的 `8/15/3` 回落到 `6/17/3`。
- 最大问题不是“把坏买入收掉了”，而是把原本正确的支撑附近偏多判断也一起压回了 `hold`：
- `2026-03-19`：`buy_on_pullback good_case -> hold bad_case`
- `2026-03-20`：`buy_on_pullback good_case -> hold bad_case`
- `2026-03-04`：`buy_on_pullback good_case -> hold bad_case`
- 说明这条规则被模型理解成了“只要还没非常明确触到最理想支撑，就优先观望”，而不是我们想要的“别在离支撑太远时乱给 buy”。
- 执行层也同步恶化到 `-4.8631`，动作分布收缩为 `buy=11 / hold=12 / sell=3`，明显比 round11 更保守。

## 下一步

- 不应继续沿这条“支撑测试 gating”规则细磨。
- 更合理的 round13 方向是回到 round11 baseline，只针对 `AAOI` 的另一类残留误差开刀：什么时候应从“催化后回调”切到真正的 `风险主导`，而不是继续扩大 `hold`。
