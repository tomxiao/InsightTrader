## 本轮目标

- 继续验证 `AAOI` 里剩余的 `中性 + 等待确认` 是否本质上已更接近“可回调参与”。

## 改动内容

- 规则层放宽了极窄的 `中性 + 等待确认 + 催化后高波动整理型` 组合。
- 证据抽取层与 `round28` 完全一致。

## 观察结论

- `AAOI` 从 `12/13/1` 提升到 `13/11/2`，准确率从 **48.0%** 提升到 **54.2%**。
- 这轮 `AAOI` 的改善非常明显，说明 `等待确认` 确实是它在 `round28` 之后的主拖累分支。
- 到这一步，`AAOI` 已经不再是架构路线里的明显短板。

## 下一步

- 若继续优化规则层，`AAOI` 后续更值得关注的是少量 `看空 + 暂不参与 -> sell` 的误杀，而不是继续动 `等待确认`。
