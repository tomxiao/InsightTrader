## 本轮目标

- 验证 `2513.HK` 里剩余的 `中性 + 等待确认` 是否也是被过度保守地压成了 `hold`。

## 改动内容

- prompt 与 evidence 抽取保持不变。
- 规则层只新增了对极窄 `中性 + 等待确认 + 催化后高波动整理型` 的放宽。

## 观察结论

- `2513.HK` 从 `10/9/5` 提升到 `11/9/4`，准确率从 **52.6%** 提升到 **55.0%**。
- 这说明上一轮剩余的 `等待确认` 保守偏差，确实在高 beta 样本里继续拖累主指标。
- 当前 `2513.HK` 的剩余坏样本主要已经转移到：
- `看空 + 暂不参与 -> sell`
- 以及少量 `中性 + 暂不参与 -> hold`

## 下一步

- 如果继续做 rules-only round，`2513.HK` 最值得再打的是 `中性 + 暂不参与 + 边际走弱` 这组残余样本。
