## 本轮目标

- 在只改规则层的前提下，尽量保持 `MU` 不被新规则外溢伤害。

## 改动内容

- 放宽了 `decision_rules.py` 中两类组合：
- `看多 + 等待回调 + 高风险`
- `中性 + 等待回调`
- prompt 与 evidence schema 保持不变，继续沿用 `round26` 报告。

## 观察结论

- `MU` 本轮依然是 `10/11/5`，准确率 **47.6%**，和 `round26` 完全一致。
- 这说明本轮 rule-only 调整几乎没有对 `MU` 产生负外溢，是一次比较干净的修正。
- 也说明 `MU` 当前不再是规则层的主要瓶颈项。

## 下一步

- 后续若继续优化规则层，`MU` 更适合作为 guardrail 样本，而不是主攻样本。
