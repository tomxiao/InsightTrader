## 本轮目标

- 在继续只修 `2513.HK` 局部残差的前提下，确保 `AAOI` 不被新规则外溢伤害。

## 改动内容

- prompt 与 evidence 抽取继续沿用 `round29`。
- 规则层只新增了极窄的 `中性 + 暂不参与 + 催化后高波动整理型 + 边际走弱 + 高风险` 放宽。

## 观察结论

- `AAOI` 本轮继续保持 `13/11/2`，准确率 **54.2%**，与 `round29` 完全一致。
- 这说明本轮规则命中范围非常干净，没有把 `AAOI` 再次拖回过度偏多或过度保守。

## 下一步

- `AAOI` 当前已不再是整体主指标的瓶颈，后续更适合作为稳定对照样本保留。
