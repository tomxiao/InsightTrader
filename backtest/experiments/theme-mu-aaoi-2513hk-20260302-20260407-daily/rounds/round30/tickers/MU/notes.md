## 本轮目标

- 在继续只改规则层、且只修 `2513.HK` 残余语境的前提下，确保 `MU` 不被外溢伤害。

## 改动内容

- prompt 与 evidence schema 完全沿用 `round29`。
- 规则层只新增了极窄的 `中性 + 暂不参与 + 催化后高波动整理型 + 边际走弱 + 高风险` 放宽。

## 观察结论

- `MU` 本轮继续保持 `11/10/5`，准确率 **52.4%**，与 `round29` 完全一致。
- 这说明本轮规则几乎只命中了目标残余样本，没有对 `MU` 产生任何新增副作用。

## 下一步

- `MU` 现在已经更像 guardrail 样本，不需要再优先单独修。
