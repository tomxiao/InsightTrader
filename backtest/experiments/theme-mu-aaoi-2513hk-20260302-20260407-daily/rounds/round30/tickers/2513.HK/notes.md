## 本轮目标

- 只修 `2513.HK` 剩余的 `中性 + 暂不参与 + 边际走弱` 残余 bad case，验证能否把全主题正式推过 `55%`。

## 改动内容

- prompt 与 evidence schema 完全不动。
- 规则层只新增了极窄的 `中性 + 暂不参与 + 催化后高波动整理型 + 边际走弱 + 高风险` 放宽。

## 观察结论

- `2513.HK` 从 `11/9/4` 大幅提升到 `14/7/3`，准确率从 **55.0%** 提升到 **66.7%**。
- 这轮几乎完全验证了我们的归因判断：`round29` 之后的主要残差，确实就是这批被保守压成 `hold` 的高 beta 反弹前样本。
- 同时 `MU / AAOI` 都没有退步，所以这是一次非常干净、非常高价值的局部规则修复。

## 下一步

- `2513.HK` 仍有少量 `看空 + 暂不参与 -> sell` 的误杀，但在主题目标已经达成后，不再适合继续盲目放大规则。
