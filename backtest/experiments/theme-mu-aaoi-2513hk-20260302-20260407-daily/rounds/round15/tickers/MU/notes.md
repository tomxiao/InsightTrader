## 本轮目标

- 验证“上游共识 gating”能否在保住 `MU` 改善的同时，修回 `2513.HK`。

## 改动内容

- 把 round14 的 guardrail 收窄为：只有上游报告形成更强的“回调参与”共识时才触发。

## 观察结论

- `MU` 从 `10/11/5` 退到 `9/11/6`。
- 主要退步不是错误卖出，而是部分样本重新回到 `hold`，例如 `2026-04-02` 从 `buy good` 退成 `hold unclear`。
- 说明这条 `共识 gating` 对 `MU` 来说已经过度保守。

## 下一步

- 若还要保留 `MU` 的例外，不能再要求太强的上游共识。
- 更合理的方向是直接识别“公司特异性催化 + 技术回调参与”的场景，而不是提高共识门槛。
