## 本轮目标

- 观察 `共识 gating` 是否会意外把 `AAOI` 压回 `hold`。

## 改动内容

- 承接 round15 的全局共识型 guardrail，没有单独加 `AAOI` 规则。

## 观察结论

- `AAOI` 从 `11/13/2` 退回 `9/14/3`。
- `2026-03-02`、`2026-03-05`、`2026-03-09` 这几天从原本偏多样本被重新压成 `hold bad_case`，是这轮的主要退步来源。
- 说明 `共识 gating` 对 `AAOI` 这种高波动催化股也太严格了。

## 下一步

- `AAOI` 不能继续吃“提高共识门槛”的规则。
- 若继续修它，更适合从“公司催化是否已实质改变 1-2 周方向”这条线上单独定义。
