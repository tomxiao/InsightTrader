# round15

## 本轮目标

- 回到 `round13` 基线，收窄 `round14` 的 guardrail。
- 核心假设：只有上游报告已形成“回调参与 / buy on weakness”共识时，才允许在财报前或宏观噪音期继续保持 `趋势延续`。

## 跨标的总结

- `round15` 全主题达到 `good/bad = 30/33`，方向准确率 `47.6%`，仍低于 `round13`。
- `2513.HK` 明显修回，从 `10/10` 恢复到 `12/8`；`2026-03-06` 和 `2026-03-30` 的偏多样本重新回到 `buy_on_pullback good_case`。
- 代价是 `MU` 退到 `9/11`，`AAOI` 退到 `9/14`，主要坏模式是把原本应继续偏多的早期样本重新压回 `hold`。
- 结论：`共识 gating` 能缓解 `2513.HK` 的副作用，但会让美股趋势样本重新过度保守，不适合作为下一条全局规则。
- 下一轮若继续验证，应改成“公司特异性催化驱动”的更窄版本，只让有明确经营/订单/盈利催化支撑的样本触发这条例外。
