# round19

## 本轮目标

- 在 round18 失败后，把 MU 规则继续收窄成真正的 tie-breaker：
- 只有“公司特异性催化仍是主线 + 关键支撑未破 + 正文明确写回调参与 + 不是催化刚落地后的过热消化”四项同时满足时，才允许额外偏向 `趋势延续 -> 择机买入`。

## 跨标的总结

- round19 仍然是失败 round，总体 `good/bad/unclear = 24/40/12`，方向准确率 **37.5%**，和 round18 一样差。
- `MU` 比 round18 略有回收，来到 `9/10/7`，说明 tie-breaker 的确比上一轮少了外溢，但修复力度也不够，仍然低于 round13 的 `8/12/6` 与 round11 的 `11/10/5` 目标区间。
- `AAOI` 几乎没有恢复，仍是 `6/17/3`，说明即使把 MU 条件继续收窄，当前这条逻辑仍然会把 AAOI 的关键样本推回 `hold`。
- `2513.HK` 进一步退到 `9/13/2`，而且实际成交掉到 `0`，港股 guardrail 明显被破坏。
- 结论：问题已经不是“MU 规则还不够窄”，而是这条 prompt 方向本身很难在不伤到其他样本的前提下带来净改进。
