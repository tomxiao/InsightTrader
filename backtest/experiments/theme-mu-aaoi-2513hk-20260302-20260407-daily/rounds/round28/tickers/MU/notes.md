## 本轮目标

- 在这里填写该标的本轮重点。

## 改动内容

- 在这里填写本轮运行前做了哪些调整。

## 观察结论

- 在这里填写复盘后的主要发现。

## 下一步

- 在这里填写下一步动作。
## 本轮目标

- 在继续只改规则层的前提下，不让新增规则对 `MU` 造成外溢伤害。

## 改动内容

- prompt 与 evidence schema 完全沿用 `round27`。
- 规则层只额外放宽了极窄的一类 `中性 + 暂不参与` 组合。

## 观察结论

- `MU` 本轮是 `10/11/5`，准确率 **47.6%**，与 `round27` 完全一致。
- 这说明本轮新增规则几乎没有对 `MU` 产生新的负外溢，属于一次非常干净的修正。
- `MU` 当前剩余问题不在 `no_entry`，而更多集中在：
- 一批 `看多 + 等待回调` 的错误买入
- 以及少量 `中性 + 等待确认` 被压成 `hold bad_case`

## 下一步

- 后续若继续 rule-only 迭代，`MU` 仍更适合作为 guardrail 样本，而不是主攻样本。
