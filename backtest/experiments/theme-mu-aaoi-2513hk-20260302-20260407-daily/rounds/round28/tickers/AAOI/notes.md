## 本轮目标

- 在这里填写该标的本轮重点。

## 改动内容

- 在这里填写本轮运行前做了哪些调整。

## 观察结论

- 在这里填写复盘后的主要发现。

## 下一步

- 在这里填写下一步动作。
## 本轮目标

- 在不改 prompt 的前提下，继续修复 `AAOI` 被过度压成 `中性 / hold` 的剩余 bad case。

## 改动内容

- 规则层新增了对极窄 `中性 + 暂不参与` 组合的放宽。
- 证据抽取层与 `round27` 完全一致。

## 观察结论

- `AAOI` 本轮继续保持在 `12/13/1`，准确率 **48.0%**，与 `round27` 持平。
- 这说明这次 rule-only 调整没有伤到 `AAOI`，同时也表明 `AAOI` 当前主要拖累已经从 `no_entry` 转移到了更窄的 `等待确认` 分支。
- 具体看，剩余坏样本里最值得下一步继续研究的是：
- `中性 + 等待确认 + 催化后高波动整理型`
- 这组在 `AAOI` 里仍有多天表现为 `hold bad_case`

## 下一步

- 若继续优化规则层，`AAOI` 最值得验证的不是继续放宽 `no_entry`，而是研究哪些 `等待确认` 本质上已经更接近“可回调参与”。
