## 本轮目标

- 验证“公司特异性催化 + 技术回调参与”能否成为 `MU` 的更窄 guardrail。

## 改动内容

- 把 round15 的共识 gating 改成“技术报告 + 公司特异性催化”的组合条件。

## 观察结论

- `MU` 提升到 `11/9/6`，是这次 3-round sprint 里单票最好的 `MU` 结果。
- 说明 `MU` 确实更吃“公司催化明确、财报前等待更好位置参与”这类规则。
- 但执行层平均收益仍然偏弱，且这条规则并不能作为全局方案成立。

## 下一步

- `MU` 的这条规则更像一个分群专用 guardrail，而不是主题级规则。
- 若后续要继续冲 `MU`，应从 `round13` 单独开分支验证，不要再和 `AAOI / 2513.HK` 绑在同一条全局 prompt 上。
