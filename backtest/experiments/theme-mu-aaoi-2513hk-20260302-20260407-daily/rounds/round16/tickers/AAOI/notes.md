## 本轮目标

- 观察“公司特异性催化 gating”是否能让 `AAOI` 保留偏多样本而不再过度保守。

## 改动内容

- 承接 round16 的全局公司催化 guardrail，没有单独补 `AAOI` 规则。

## 观察结论

- `AAOI` 从 `9/14/3` 直接退到 `6/17/3`，是 round16 最大的失败来源。
- 主要坏模式是大面积 `hold bad_case`，尤其是 `2026-03-02`、`2026-03-04`、`2026-03-05`、`2026-03-09` 被压回观望。
- 这说明“公司特异性催化”这条定义对 `AAOI` 来说过窄，直接把 prompt 推回了保守侧。

## 下一步

- 这条路径已经不适合再在 `AAOI` 上继续试。
- 若后续继续修 `AAOI`，必须回到它自己的残留 bad case，而不是继续用 `MU` 导出的规则套进去。
