## 本轮目标

- 作为 guardrail，验证 `MU` 的健康回调规则是否会误伤港股高波动样本。

## 改动内容

- 未对 `2513.HK` 单独加规则，只承接 round14 的全局 prompt 变化。

## 观察结论

- `2513.HK` 从 `15/8/1` 明显回落到 `10/10/4`，是 round14 的主要代价样本。
- 关键坏变化是 `2026-03-04`、`2026-03-06`、`2026-03-30` 等原本正确的偏多样本被压回 `hold`。
- 说明 round14 的 guardrail 在港股高波动样本上发生了方向外溢。

## 下一步

- 下一轮必须从 `round13` 重新出发，而不是继承 round14。
- 如果还要保留这条 guardrail，就必须加更强的适用边界，避免港股样本继续被误压。
