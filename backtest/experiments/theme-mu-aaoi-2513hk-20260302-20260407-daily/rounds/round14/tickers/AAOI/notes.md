## 本轮目标

- 观察 `MU` guardrail 是否会意外伤到 `AAOI`。

## 改动内容

- 未针对 `AAOI` 单独加规则，只承接 round14 的全局 prompt 变化。

## 观察结论

- `AAOI` 从 `10/14/2` 小幅改善到 `11/13/2`。
- 正向变化主要来自早期样本继续偏多，例如 `2026-03-05` 保持为 `buy_on_pullback good_case`。
- 说明这条 guardrail 对 `AAOI` 没有立即造成伤害，但也没有解决其后段 bad case 集中问题。

## 下一步

- `AAOI` 不适合继续吃 `MU` 的全局规则红利。
- 后续要单独修时，应回到它自己的“催化后首轮回撤”与“hold 滥用”问题，而不是跟着 `MU` 一起调。
