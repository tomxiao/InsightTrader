# 2026-0421-round04

## 本轮目标

- 这轮仅做 `decision_only_round`，复用 round03 的 `1_analysts`，只微调 `decision manager`。
- 主要目标是：收紧 `MU` 的偏弱买入，补足 `AAOI` 的风险主导卖出，同时观察 `2513.HK` 是否被压得过度保守。

## 跨标的总结

- `MU`：买入信号从 `12` 降到 `8`，卖出从 `4` 升到 `5`，平均收益从 `-5.5512` 改善到 `-4.8567`，方向正确但仍偏弱。
- `AAOI`：买入信号从 `14` 降到 `11`，卖出从 `1` 升到 `2`，平均收益从 `-0.0198` 提升到 `8.0742`，是本轮最明显的正向样本。
- `2513.HK`：买入从 `8` 压到 `3`，实际成交从 `1` 变成 `0`，说明这轮对港股样本过于保守。
- 结论：本轮改动对美股样本有效，但对港股 guardrail 有副作用；下一轮应保留美股收口逻辑，同时单独修复 `2513.HK` 的过度观望。
