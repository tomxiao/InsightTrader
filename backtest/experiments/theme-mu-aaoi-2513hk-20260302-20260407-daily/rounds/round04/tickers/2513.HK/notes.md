## 本轮目标

- 把 `2513.HK` 作为港股 guardrail，检查本轮美股导向的 decision manager 调整是否会把港股样本压得过于保守。

## 改动内容

- 复用 round03 的 analyst 报告，只重跑 decision manager。
- 沿用了与 `MU / AAOI` 相同的“收弱买入、补风险主导卖出”规则。

## 观察结论

- `buy_on_pullback` 从 `8` 下降到 `3`，`hold` 从 `9` 上升到 `15`，`sell` 基本持平（`7 -> 6`）。
- 实际成交从 `1` 变成 `0`，`good_case` 从 `8` 降到 `4`，`bad_case` 从 `13` 升到 `16`。
- 说明这轮规则对港股样本过于保守，已经把原本还能两头分布的样本明显压回了 `保持观望`。

## 下一步

- 下一轮需要专门给港股 / 高波动样本补一条防过度保守规则。
- 目标不是让 `2513.HK` 更激进，而是避免它被机械压回大量 `hold`。
