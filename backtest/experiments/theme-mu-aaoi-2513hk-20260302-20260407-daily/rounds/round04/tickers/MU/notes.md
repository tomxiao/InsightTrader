## 本轮目标

- 收紧 `MU` 上偏弱、偏早的 `择机买入`，减少只因“支撑在附近 / 长期趋势未坏”就触发的买点。
- 同时观察 `建议卖出` 是否能略微恢复，而不是全部堆到 `保持观望`。

## 改动内容

- 复用 round03 的 `1_analysts`，仅重跑 `2_decision/summary.md`。
- decision manager 新增两类约束：
- 对缺少 news/fundamentals 正向确认、也缺少新企稳证据的样本，不再轻易判为 `趋势延续`。
- 对已经进入减仓 / 风险控制 / 反弹卖出语义的样本，提高 `风险主导` 优先级。

## 观察结论

- `buy_on_pullback` 从 `12` 降到 `8`，`hold` 从 `10` 升到 `13`，`sell` 从 `4` 升到 `5`。
- 平均收益从 `-5.5512` 改善到 `-4.8567`，`good_case` 从 `2` 升到 `3`，`bad_case` 从 `18` 降到 `17`。
- 说明“收弱买入、补一点卖出”这个方向是有效的，但当前仍然不够，`MU` 还是这组样本里最弱的一只。

## 下一步

- 保留本轮对 `MU` 的收口逻辑。
- 下一轮重点看还有哪些日期仍然被误判为 `择机买入`，继续压缩弱证据买点。
