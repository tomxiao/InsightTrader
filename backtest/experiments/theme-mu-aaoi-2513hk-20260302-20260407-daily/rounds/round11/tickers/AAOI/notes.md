## 本轮目标

- 修复 `AAOI` 在“公司级催化已改变预期，但短期高位整理”场景下被错误降到 `hold` 或 `sell` 的问题。

## 改动内容

- 在 round10 的 signal-first 骨架上，只新增一条窄规则：
- 若报告明确描述的是订单落地、商业化验证、客户突破等公司级催化后的高位整理，只要关键支撑未破、正文仍倾向等待更好参与位，就优先保留 `趋势延续 -> 择机买入`。

## 观察结论

- `AAOI` 从 round10 的 `6/18/2` 回升到 `8/15/3`，准确率从 `25.0%` 回到 `34.8%`，等于 round08。
- 最关键的修复是：
- `2026-03-19`：`hold bad_case -> buy_on_pullback good_case`
- `2026-03-23`：`hold bad_case -> buy_on_pullback good_case`
- `2026-04-02`：`hold bad_case -> buy_on_pullback good_case`
- 说明这轮改动确实命中了我们想修的那类“偏多修复中的等待参与”误判。
- 但这只票仍然没有彻底修好，残留问题主要有两类：
- `2026-03-09` 依然停在 `sell bad_case`
- `2026-03-13`、`2026-03-30` 虽然从 `hold` 切到了 `buy_on_pullback`，但仍然是 `bad_case`
- 执行层只小幅改善到 `-1.6337`，说明信号修复还没有完全传导成更好的交易结果。

## 下一步

- 如果继续开 round12，`AAOI` 仍然是最值得单点深挖的样本。
- 重点不再是笼统减少 `hold`，而是只修“催化后首轮转弱究竟该卖还是等二次参与”的边界。
