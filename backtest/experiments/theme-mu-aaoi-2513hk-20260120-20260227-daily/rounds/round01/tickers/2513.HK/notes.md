## 本轮目标

- 验证 `2513.HK` 在新时间窗里的 guardrail 是否仍能成立，并观察高 beta 港股样本是否会成为时间外泛化的主要薄弱环节。

## 改动内容

- 本轮不做额外调优。
- 完整沿用来源主题 `round30` 的 evidence schema 与 `decision_rules.py`。

## 观察结论

- `2513.HK` 本轮为 `9 / 12 / 5`，方向准确率 **42.9%**。
- 它明显落后于 `MU / AAOI`，是当前新主题的主要拖累。
- 从结构归因看，问题主要集中在：
- `Beta情绪技术驱动型` 只有 **20.0%**
- `看空 / 风险主导` 分支准确率为 **0.0%**
- 这说明当前规则层在原主题里对 `2513.HK` 修回的那部分逻辑，并没有在这个更早的时间窗里完全泛化。

## 下一步

- 如果继续在本主题里开 `round02`，最值得优先修的是 `2513.HK` 的 `Beta情绪技术驱动型` 与 `看空 / 风险主导` 残余误判。
